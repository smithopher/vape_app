<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class='navbar-brand' id="brand" href='/'>VapeStack</a>
    </div>
    <% if current_user %>

      <form id="search-bar" class='nav navbar-form navbar-nav' method="get" action="/search">
        <input name="authenticity_token" type="hidden" value='<%=form_authenticity_token%>'/>
        <input class="form-control" placeholder="Search for user" type="text" name="search"/>
        <input class="btn btn-default" type="submit" value="Search" />
      </form>
    <% end %>
    <div id="login-button" class="nav navbar-nav navbar-right">
      <% if current_user %>
        <a href='/logout'><button class='btn btn-default navbar-btn'>Sign out</button></a>
      <% else %>
        <a href='/signup'><button class='btn btn-default navbar-btn'>Sign up!</button></a>
      <% end %>
    </div>
  </div>
</nav>
